<div class="viewAct">
    <H1 class="title">Activities</H1>
    <div class="tile is-ancestor activity-card">
        {{#each user}}
        <article class="tile is-child">
            <p class="title">{{user.name}}'s Activity:{{activityName}}</p>
            <p class="subtitle">Date:{{user.date}}</p>
            <div class="content">
                <ul>
                    <li>Activity Description: {{user.activityDes}}</li>
                    <li>Rigor: {{user.rigor}}</li>
                    <li>Mileage: {{user.mileage}}</li>
                    <li>Duration: {{user.duration}}</li>
                </ul>
            </div>
        </article>
        {{/each}}
    </div>
</div>